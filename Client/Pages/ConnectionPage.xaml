<Page
    x:Class="Client.Pages.ConnectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollViewer>
        <StackPanel Padding="20" Spacing="20">
            <Button Content="← Retour" Click="Back_Click" HorizontalAlignment="Left"/>
            <TextBlock Text="Connexion" FontSize="24" FontWeight="Bold"/>
            <Border CornerRadius="8" Padding="20">
                <StackPanel Spacing="10">
                    <TextBlock Text="Adresse du serveur"/>
                    <TextBox x:Name="AddressBox" Text="{x:Bind ServerAddress, Mode=TwoWay}"/>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Content="Chercher un serveur" Click="Search_Click"/>
                        <Button Content="Sauvegarder" Click="Save_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
