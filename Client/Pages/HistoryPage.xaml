<Page
    x:Class="Client.Pages.HistoryPage"
    x:Name="PageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:data="using:Client.Models">
    <Page.Resources>
        <!-- Templates pour les patients non archivés -->
        <DataTemplate x:Key="PatientDefaultTemplate" x:DataType="data:Patient">
            <Border Background="{x:Bind Colors}" CornerRadius="10" Margin="0,4">
                <Border.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="{x:Bind ToggleExamLabel}" Tag="{x:Bind}" Click="TogglePatientTaken_Click" />
                        <MenuFlyoutItem Text="Infos" Tag="{x:Bind}" Click="InfoPatient_Click" />
                        <MenuFlyoutItem Text="Modifier" Tag="{x:Bind}" Click="EditPatient_Click" />
                        <MenuFlyoutItem Text="Supprimer" Tag="{x:Bind}" Click="DeletePatient_Click" />
                        <MenuFlyoutSubItem Text="Modification temps" Visibility="{Binding TimeModificationVisibility, ElementName=PageRoot}">
                            <MenuFlyoutItem Text="Mettre en premier" Tag="{x:Bind}" Click="MovePatientFirst_Click" />
                            <MenuFlyoutItem Text="Monter d'un cran" Tag="{x:Bind}" Click="MovePatientUp_Click" />
                            <MenuFlyoutItem Text="Descendre d'un cran" Tag="{x:Bind}" Click="MovePatientDown_Click" />
                        </MenuFlyoutSubItem>
                    </MenuFlyout>
                </Border.ContextFlyout>
                <Grid Padding="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="{x:Bind Title}" FontWeight="Bold" Foreground="{x:Bind ForegroundColor}"/>
                        <TextBlock Text="{x:Bind LastName}" Margin="4,0" Foreground="{x:Bind ForegroundColor}"/>
                        <TextBlock Text="{x:Bind FirstName}" Foreground="{x:Bind ForegroundColor}"/>
                    </StackPanel>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind Examinator}" Foreground="{x:Bind ForegroundColor}" HorizontalAlignment="Right"/>
                    <!-- Deuxième ligne : examens, œil, annotation et heure -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="{x:Bind Exams}" FontWeight="Bold" Foreground="{x:Bind ForegroundColor}"/>
                        <TextBlock Text="{x:Bind Eye}" Foreground="{x:Bind ForegroundColor}"/>
                        <TextBlock Text="{x:Bind Annotation}" Foreground="{x:Bind ForegroundColor}"/>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind HoldTimeFormatted}" Foreground="{x:Bind ForegroundColor}" FontWeight="Bold" HorizontalAlignment="Right"/>
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="PatientTakenTemplate" x:DataType="data:Patient">
            <Border Background="Gray" CornerRadius="10" Margin="0,4">
                <Border.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="{x:Bind ToggleExamLabel}" Tag="{x:Bind}" Click="TogglePatientTaken_Click" />
                        <MenuFlyoutItem Text="Infos" Tag="{x:Bind}" Click="InfoPatient_Click" />
                        <MenuFlyoutItem Text="Modifier" Tag="{x:Bind}" Click="EditPatient_Click" />
                        <MenuFlyoutItem Text="Supprimer" Tag="{x:Bind}" Click="DeletePatient_Click" />
                    </MenuFlyout>
                </Border.ContextFlyout>
                <Grid Padding="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="{x:Bind Title}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind LastName}" Margin="4,0"/>
                        <TextBlock Text="{x:Bind FirstName}" />
                    </StackPanel>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind PickUpTimeFormatted}" HorizontalAlignment="Right"/>
                </Grid>
            </Border>
        </DataTemplate>

        <!-- Templates pour les patients archivés -->
        <DataTemplate x:Key="ArchivedPatientDefaultTemplate" x:DataType="data:Patient">
            <Border Background="{x:Bind Colors}" CornerRadius="10" Margin="0,4">
                <Border.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Désarchiver" Tag="{x:Bind}" Click="UnarchivePatient_Click" />
                        <MenuFlyoutItem Text="Infos" Tag="{x:Bind}" Click="InfoPatient_Click" />
                        <MenuFlyoutItem Text="Supprimer" Tag="{x:Bind}" Click="DeletePatient_Click" />
                    </MenuFlyout>
                </Border.ContextFlyout>
                <Grid Padding="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="{x:Bind Title}" FontWeight="Bold" Foreground="{x:Bind ForegroundColor}"/>
                        <TextBlock Text="{x:Bind LastName}" Margin="4,0" Foreground="{x:Bind ForegroundColor}"/>
                        <TextBlock Text="{x:Bind FirstName}" Foreground="{x:Bind ForegroundColor}"/>
                    </StackPanel>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind Examinator}" Foreground="{x:Bind ForegroundColor}" HorizontalAlignment="Right"/>
                    <!-- Deuxième ligne : examens, œil, annotation et heure -->
                    <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="{x:Bind Exams}" FontWeight="Bold" Foreground="{x:Bind ForegroundColor}"/>
                        <TextBlock Text="{x:Bind Eye}" Foreground="{x:Bind ForegroundColor}"/>
                        <TextBlock Text="{x:Bind Annotation}" Foreground="{x:Bind ForegroundColor}"/>
                    </StackPanel>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind HoldTimeFormatted}" Foreground="{x:Bind ForegroundColor}" FontWeight="Bold" HorizontalAlignment="Right"/>
                </Grid>
            </Border>
        </DataTemplate>

        <DataTemplate x:Key="ArchivedPatientTakenTemplate" x:DataType="data:Patient">
            <Border Background="Gray" CornerRadius="10" Margin="0,4">
                <Border.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Désarchiver" Tag="{x:Bind}" Click="UnarchivePatient_Click" />
                        <MenuFlyoutItem Text="Infos" Tag="{x:Bind}" Click="InfoPatient_Click" />
                        <MenuFlyoutItem Text="Supprimer" Tag="{x:Bind}" Click="DeletePatient_Click" />
                    </MenuFlyout>
                </Border.ContextFlyout>
                <Grid Padding="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="4">
                        <TextBlock Text="{x:Bind Title}" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind LastName}" Margin="4,0"/>
                        <TextBlock Text="{x:Bind FirstName}" />
                    </StackPanel>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind PickUpTimeFormatted}" HorizontalAlignment="Right"/>
                </Grid>
            </Border>
        </DataTemplate>

        <data:PatientTemplateSelector x:Key="PatientTemplateSelector"
                                      DefaultTemplate="{StaticResource PatientDefaultTemplate}"
                                      TakenTemplate="{StaticResource PatientTakenTemplate}" />

        <data:PatientTemplateSelector x:Key="ArchivedPatientTemplateSelector"
                                      DefaultTemplate="{StaticResource ArchivedPatientDefaultTemplate}"
                                      TakenTemplate="{StaticResource ArchivedPatientTakenTemplate}" />


        <Style TargetType="PivotItem">
            <Setter Property="Foreground" Value="{ThemeResource PivotHeaderForeground}" />
        </Style>
        <Style x:Key="PivotHeaderItemStyle" TargetType="PivotHeaderItem">
            <Setter Property="Foreground" Value="{ThemeResource PivotHeaderForeground}" />
            <Setter Property="Margin" Value="8,0" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PivotHeaderItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition Height="2" />
                            </Grid.RowDefinitions>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="PointerOver" />
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="SelectionStates">
                                    <VisualState x:Name="Unselected">
                                        <VisualState.Setters>
                                            <Setter Target="Underline.Visibility" Value="Collapsed" />
                                        </VisualState.Setters>
                                    </VisualState>
                                    <VisualState x:Name="Selected">
                                        <VisualState.Setters>
                                            <Setter Target="Underline.Visibility" Value="Visible" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter
                                x:Name="ContentPresenter"
                                Content="{TemplateBinding Content}"
                                Foreground="{ThemeResource TextAppBackgroundColor}"
                                FontWeight="Bold"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                            <Rectangle x:Name="Underline" Grid.Row="1"
                                       Height="2"
                                       Fill="{ThemeResource AccentColor}"
                                       HorizontalAlignment="Stretch"
                                       VerticalAlignment="Bottom"
                                       Visibility="Collapsed" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Padding="10" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Content="Rafraîchir" HorizontalAlignment="Left" Click="Refresh_Click" Grid.ColumnSpan="2"/>

        <TextBlock Text="En cours" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" Margin="0,4"/>
        <TextBlock Text="Archivé" Grid.Row="1" Grid.Column="1" HorizontalAlignment="Center" Margin="0,4"/>

        <Pivot x:Name="NotArchivedPivot" Grid.Row="2" Grid.Column="0" SelectionChanged="Pivot_SelectionChanged" ItemContainerStyle="{StaticResource PivotTitleContentControlStyle}">
            <Pivot.Resources>
                <Style TargetType="PivotHeaderItem" BasedOn="{StaticResource PivotHeaderItemStyle}" />
            </Pivot.Resources>
        </Pivot>

        <Pivot x:Name="ArchivedPivot" Grid.Row="2" Grid.Column="1" SelectionChanged="Pivot_SelectionChanged" ItemContainerStyle="{StaticResource PivotTitleContentControlStyle}">
            <Pivot.Resources>
                <Style TargetType="PivotHeaderItem" BasedOn="{StaticResource PivotHeaderItemStyle}" />
            </Pivot.Resources>
        </Pivot>

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8">
            <Button Content="Archiver" Click="ArchivePatients_Click"/>
            <Button Content="Désarchiver" Click="UnarchivePatients_Click"/>
        </StackPanel>
    </Grid>
</Page>
